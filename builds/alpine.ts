import { default as alpinePlugin } from "../src/alpinePlugin.js";
import { CreateBlackberryElement, h, text, css, elementFactory, state, attribute } from "../src/element.js";

export default {
  // Alpine Plugin
  alpinePlugin,

  // JS API
  CreateBlackberryElement,
  h,
  text,
  css,
  elementFactory,

  // js decorators
  state,
  attribute,
}
